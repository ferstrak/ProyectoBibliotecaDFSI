spring.application.name=bibliotecaejercicio

spring.profiles.include=secrets

spring.threads.virtual.enabled=true

# Problem Details API (RFC 7807) - DESACTIVADO para usar nuestro GlobalExceptionHandler
spring.mvc.problemdetails.enabled=false

# Logging con formato legible
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ===================================
# PostgreSQL + Hibernate (JPA) - NeonTech
# ===================================

spring.datasource.url=jdbc:postgresql://ep-dawn-bread-amv0m085-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_h3ZFTrtQ8bWM
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraci\u00f3n de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Pool de conexiones (HikariCP)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2

